[package]
name = "api_gen"
version.workspace = true
edition = "2021"
description = "Regenerates the `nvngx-sys` Rust bindings from the DLSS SDK headers."
license = "MIT"
publish = false

[dependencies]
bindgen = "0.72"
